apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-controller-srv
  annotations:
    # Tell NGINX to parse regular expressions in paths
    nginx.ingress.kubernetes.io/use-regex: "true"
spec:
  ingressClassName: nginx
  rules:
    - host: ticketing.dev
      http:
        paths:
          - path: /api/users/?(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: auth-srv
                port:
                  number: 3000
          